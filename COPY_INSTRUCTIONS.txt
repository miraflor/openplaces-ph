OpenPlaces PH: remove temporary 0.2 CLI compatibility layer

COPY/REPLACE:
  README.md
  src/openplaces_ph/cli.py
  tests/test_cli_v03.py
  tests/test_migration_applied_v03.py

DELETE from your repo:
  src/openplaces_ph/legacy_cli.py

No matching, clustering, source-selection, checkpoint, or finalization algorithms
are changed by these replacement files.

After copying/deleting, run:
  python -m pip install -e .
  python -m pytest -q
  python -m compileall -q src tests run.py
  openplaces doctor
  openplaces
